AFFINE_URL=https://affine.example.tld
AFFINE_WORKSPACE_ID=workspace-id
AFFINE_MCP_TOKEN=aff_mcp_v1.example.secret

# Optional separate server-side authentication for ordinary AFFiNE /api/* and
# Socket.IO routes. The native aff_mcp_v1 token is intentionally limited to the
# workspace MCP endpoint and is not a normal AFFiNE session credential.
AFFINE_API_AUTH_HEADER=
AFFINE_API_AUTH_VALUE=

MCP_HTTP_TOKEN=replace-me
MCP_BIND_ADDRESS=127.0.0.1
MCP_PORT=8104
MCP_CONTAINER_NAME=affine-mcp
